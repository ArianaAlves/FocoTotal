# Variáveis de Ambiente para Desenvolvimento Local
PORT=3000
JWT_SECRET=sua_chave_secreta_jwt_muito_segura_aqui
DATABASE_URL="file:./prisma/dev.db"

# ==========================================
# Configurações para PRODUÇÃO (Render)
# ==========================================
# NODE_ENV=production
# PORT=10000
# JWT_SECRET=your_super_secret_jwt_key_here_make_it_long_and_random_123456789
# DATABASE_URL=postgresql://user:password@host:port/database
# NODE_OPTIONS=--no-deprecation --max-old-space-size=512

# IMPORTANTE: 
# - No Render, a DATABASE_URL é preenchida automaticamente se você conectou um banco PostgreSQL
# - O JWT_SECRET deve ser gerado automaticamente pelo Render (marque "Generate Value")